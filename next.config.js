/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'avatar.vercel.sh',
      'lh3.googleusercontent.com',
    ],
  },
  // ... other config options
}

module.exports = nextConfig 